build:
  box: canopytax/alpine
  steps:
    - script:
      name: print env
      code: |
        echo "here we go"
        env
        echo "---"
        echo "this is a test: 90% coverage!" > $WERCKER_REPORT_MESSAGE_FILE
        echo 'numbers --'
        echo '2334' > $WERCKER_REPORT_NUMBERS_FILE
        echo 'artifacts'
        echo '--- hello' > $WERCKER_REPORT_ARTIFACTS_DIR/bob.txt
